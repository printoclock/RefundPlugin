{% if data.state != 'completed' %}
    <form action="{{ path('sylius_refund_complete_refund_payment', {'orderNumber': data.orderNumber, 'id': data.id, 'redirectToIndex': 1}) }}" method="POST">
        <button class="button ui labeled icon yellow">
            <i class="icon check"></i>
            {{ 'sylius.ui.complete'|trans }}
        </button>
    </form>
{% endif %}
